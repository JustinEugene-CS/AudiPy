name: Publish Python Package to TestPyPI

on:
  push:
    branches:
      - main  # Change if using a different branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine
          pip install numpy opencv-python scipy

      - name: Build Package
        run: python -m build

      - name: Publish to TestPyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: pypi-AgENdGVzdC5weXBpLm9yZwIkZDFjZmE4YzctMmEwNi00MjhlLWE0ODktZDhiNTEwZDc1ZTc4AAIqWzMsImJjZmUzNWFjLTExN2MtNGRiYS05YTljLWY3YzAwYjRkN2EzZSJdAAAGIHDXU-oVXV48ia9ScMwDaUp3VfvT8-J-Yxl6V7NGLvtp
        run: twine upload --repository testpypi dist/*
